spring:
  datasource:
    url: jdbc:mysql://${S2_DB_HOST}:${S2_DB_PORT:3306}/${S2_DB_NAME}?useUnicode=true&characterEncoding=UTF-8&useSSL=false&allowMultiQueries=true&allowPublicKeyRetrieval=true
    username: ${S2_DB_USERNAME}
    password: ${S2_DB_PASSWORD}
    driver-class-name: com.mysql.jdbc.Driver